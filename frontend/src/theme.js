import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    bgLeft: "#120F13",
    bgRight: "#252329",
    customBlue: "#2F80ED",
    customBorder: "#BDBDBD",
    customBlack: "#333",
    customWhite: "#E0E0E0",
    white: "#FFF",
    tertiary: "#828282",
  },
  fonts: {
    body: "'Open Sans', sans-serif",
  },
});
